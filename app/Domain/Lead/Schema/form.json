{
  "primary": {
    "label": "Primary Information",
    "fields": [
      { "key": "first_name", "required": true },
      { "key": "last_name", "required": true },
      { "key": "email", "required": false },
      { "key": "phone", "required": false },
      { "key": "mobile", "required": false }
    ]
  },

  "company_info": {
    "label": "Company Information",
    "fields": [
      { "key": "company", "required": false },
      { "key": "position", "required": false },
      { "key": "title", "required": false }
    ]
  },

  "lead_details": {
    "label": "Lead Details",
    "fields": [
      { "key": "lead_type", "required": false },
      { "key": "lead_stage", "required": true },
      { "key": "lead_rating", "required": false },
      { "key": "budget_min", "required": false },
      { "key": "budget_max", "required": false },
      { "key": "boat_type_interest", "required": false },
      { "key": "timeline_to_buy", "required": false },
      { "key": "trade_in", "required": false },
      { "key": "communication_preference", "required": false },
      { "key": "lead_source_detail", "required": false }
    ]
  },

  "address": {
    "label": "Address",
    "is_address": true,
    "fields": [
      { "key": "address_line_1", "required": false },
      { "key": "address_line_2", "required": false },
      { "key": "city", "required": false },
      { "key": "state", "required": false },
      { "key": "postal_code", "required": false },
      { "key": "country", "required": false }
    ]
  },

  "notes": {
    "label": "Notes",
    "fields": [
      { "key": "initial_notes", "required": false },
      { "key": "objections", "required": false },
      { "key": "interests", "required": false }
    ]
  },

  "management": {
    "label": "Management",
    "fields": [
      { "key": "status_id", "required": true },
      { "key": "source_id", "required": false },
      { "key": "priority_id", "required": false },
      { "key": "assigned_user_id", "required": false },
      { "key": "last_contacted_at", "required": false },
      { "key": "next_followup_at", "required": false },
      { "key": "converted_contact_id", "required": false }
    ]
  }
}
